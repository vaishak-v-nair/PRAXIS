---
description: What did this session just cost, in API-equivalent dollars
---

Run `praxis cost` with the Bash tool (add `--all` if I asked for every session)
and show me the output. Then explain it in one short paragraph of plain
English: what the number covers, that it is an API-equivalent estimate — not a
bill — and whether anything about the trend deserves attention.

If the command is not found, tell the user to install it:
`npm install -g praxis-memory`
