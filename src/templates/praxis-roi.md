---
description: The receipt — sessions, commits, hours, and dollars
---

Run `npx -y praxis-memory roi` with the Bash tool (add `--days N` if I asked for a specific
window) and show me the output. Then summarize it in one short paragraph of
plain English: what the AI work added up to over the period, and the one number
most worth knowing.
